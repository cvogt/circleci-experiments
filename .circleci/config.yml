version: 2
jobs:
  build:
    docker:
      - image: circleci/node:4.8.2
    steps:
      - checkout
      - run: |
        FOO=5
        echo $FOO
      - run: |
      echo $FOO
workflows:
  version: 2
  build_and_test:
    jobs:
      - build
